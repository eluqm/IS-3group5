pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        mavenCentral()
        google()

        maven { url "https://jitpack.io" } // Implementacion del BarChart
    }
}
rootProject.name = "IS3 Tests Project"
include ':app'
